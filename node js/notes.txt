node js
---------

   ->node js is the run time server side javascript runtime environment
   ->using node js we can build server (http core model)

   run in js file:
   command ->node filename

   user defined modules:
     ->any function can be defined by user
     ->export modules from one file to another file
              syntax:module.export = moduleName
                     module.exports={moduleName1, moduleName2...}


     ->import exported module
            syntax: const {moduleName1, moduleName2}=require("fileName")




    userDetails=[
        {name:'ravi',email:'ravi@gmail.com'},
        {name:'sai',email:'sai@gmail.com'},

    ]

    productprice=[1292,522,5026,8944]



    node core inbuilt modules(core modules):
    ----------------------------------------

       ->core modules is the inbuilt  




1.fs module(file system):
     fs module allows reading,writing,deleting files

     fs module in build methods:
     1. fs read file()
     2. fs write file()
     3. fs append file()
     4. fs unlink()

2.path module:
      ->helps in working with file paths (folder navigation,extensions,filenames)
    1.path.join()
    2.path.resolve()
    3.path.extname()
    4.path basename()

3.os
4.http:
   ->used to create 





